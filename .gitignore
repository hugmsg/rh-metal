# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
*.swp

# Vercel
.vercel/

# Claude Code local settings
.claude/settings.local.json

# Backup files (ne pas versionner les backups JSON)
rh-metal-backup-*.json

# Python (nfc-bridge)
__pycache__/
*.pyc
venv/

# Build de l'exécutable nfc-bridge (PyInstaller) — binaire lourd, régénéré à
# la demande (pip install pyinstaller && pyinstaller --onefile nfc_bridge.py),
# pas versionné. Distribué via la synchronisation Google Drive de ce dossier.
nfc-bridge/build/
nfc-bridge/dist/

.vercel
